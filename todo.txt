++ Update call log system :

	* Use structure to pass string to write (struct yall_call_log_data)
	* Format message through helper functions :
		* set header
		* add line (indent)
		* prepare call data
		* free call dat
struct yall_call_log_data:
	header
	message * { int depth, char *content, next }

OPAQUE STRUCTURE
yall_call_data *
void func(struct yall_call_log_data *d, void *args)
{
	yall_call_set_header(d, "Debg info");
	yall_call_add_line(d, 0, "format");
}
